{
  "name": "puppeteer-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec ./node_modules/.bin/ts-node src/index.ts",
    "checkin-lib": "git add lib"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/puppeteer": "^1.12.3",
    "nodemon": "^1.18.11",
    "pre-commit": "^1.2.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "puppeteer": "^1.14.0"
  },
  "pre-commit": [
    "build",
    "checkin-lib"
  ]
}
