PR Notes - 2025-02-26
    "types.ts" file 
        // To support the "SortBy" and "SortOrder" CLI options
        Added Lines: 6 - 18
            //  Added SortBy and SortOrder for use with the Command Line options
            export enum SortBy {
            author = "author",
            date = "date",
            title = "title"
            }

            export enum SortOrder {
            asc = "asc",
            desc = "desc"
            }

        Added Lines:  33 - 34
            //  Added SortBy and SortOrder for use with the Command Line options
            sortBy?: SortBy;
            sortOrder?: SortOrder;

    "index.ts" file:    
        // To support the "SortBy" and "SortOrder" CLI options
            Added Lines:  172 - 186
                //  Convert the sortOrder command-line value to a String value
                let sortOrder:string;

                switch(options.sortOrder) {
                    case SortOrder.asc:
                    sortOrder="ASCENDING";
                    break;
                    case SortOrder.desc:
                    sortOrder="DESCENDING";
                    break;
                    default:
                    sortOrder="DESCENDING";
                }
            
            Changed Lines:  214 - 216            
                From:
                    sortOrder: "DESCENDING",
                    sortIndex: "TITLE",
                To:
                    sortOrder: sortOrder,
                    sortIndex: options.sortBy,

            Added Lines: 654 - 665            
                .option("sortBy", {
                    default: SortBy.title,
                    description: "What value to sort books on (Author, Date or Title",
                    choices: Object.values(SortBy),    
                })
                .option("sortOrder", {
                    default: SortOrder.desc,
                    description: "What order to sort books by (Ascending or Descending",    
                    choices: Object.values(SortOrder)
                })


        // To add Error.Message to the string written to the "../failed-books.txt" file
            Changed Line2: 515 - 519       
                From:  
                    const failedBooksContent = failedBooks.map((b) => b.book.title).join("\n");          
                To:
                    //  Added the "Error.Message" to the Output so that the reason for the failure is shown    
                    const failedBooksContent = failedBooks.map((b) => b.book.title + " : " + b.error.message).join("\n");
  
    README.md
        Added:
                Descriptions for the "SortBy" and "SortOrder" CLI arguments        